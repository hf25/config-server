spring:
  datasource:
    url: jdbc:mysql://47.75.90.35:3306/ezeeship-message-center?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: ezeeship1108
    driver-class-name: com.mysql.jdbc.Driver
    hikari:
      maximum-pool-size: 5
  redis:
            database: 0
    # server host1
            host: 127.0.0.1
    # server password
    #        password:
    #connection port
            port: 6379
    # pool settings ...
            lettuce:
              pool:
                min-idle: 0
                max-active: 20
                max-idle: 20
  mail:
    properties:
      mail.smtp.connectiontimeout: 5000
      mail.smtp.timeout: 3000
      mail.smtp.writetimeout: 5000
      mail.smtp.auth: true
      mail.smtp.ssl.enable: true
      # mail.smtp.port: 465
    host: smtp.gmail.com
    username: support@ezeeship.com
    password: ezlp$2000
    port: 465
eureka:
  client:
    enabled: false
